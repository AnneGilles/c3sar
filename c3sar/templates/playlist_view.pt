<html xmlns="http://www.w3.org/1999/xhtml"
      xmlns:tal="http://xml.zope.org/namespaces/tal"
      xmlns:metal="http://xml.zope.org/namespaces/metal"
      metal:use-macro="base">

<tal:block metal:fill-slot="content">
  <p>you are ${viewer_username}.

    <p>
      <a href="${prev_id}">previous</a> | 
      <a href="/playlist/list">all</a> |
      <a href="${next_id}">next</a> |
      <a href="/playlist/edit/${playlist.id}">edit</a> |
      <a href="/playlist/rm/${playlist.id}">delete</a> 
    </p>

    <h1>Information about playlist "${playlist.name}"</h1>

    <p>Issuer: <b class="hits">${playlist.issuer}</b>.</p>

</tal:block>

</html>


